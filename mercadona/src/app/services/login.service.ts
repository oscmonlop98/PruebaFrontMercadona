import { Injectable } from '@angular/core';
import { ToolbarComponent } from '../components/toolbar/toolbar.component';

@Injectable({
  providedIn: 'root'
})
export class LoginService {

  constructor() { }

  // setLogin(log: boolean) {
  //   this.toolbarComponent.recieveData(log);
  // }

}
